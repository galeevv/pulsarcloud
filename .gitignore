# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/.next-http-e2e/
/out/

# Local source variants; the app ships the selected transparent PNG only.
/public/logo/logo-no-bg-preview (carve.photos).jpg
/public/logo/logo-no-bg-preview (carve.photos).psd
/public/logo/logo-no-bg-preview (carve.photos).webp

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example
prisma/*.db
prisma/*.db-wal
prisma/*.db-shm

# local output artifacts
stdout
*.db
*.db-shm
*.db-wal
*.db-journal

# local database, export, and backup directories
prisma/dev.db
data/
backups/

# typescript
*.tsbuildinfo
next-env.d.ts

# generated Prisma Client (created by npm run db:generate)
/src/generated/prisma/

# infrastructure secrets — never commit private keys, tokens, or rendered envs
*.pem
*.key
id_ed25519*
*_ed25519
!*_ed25519.pub
infra/**/.env
infra/**/*.local.env
infra/**/secrets/
infra/**/rendered/
!infra/**/.env.example

# Local infra handoff (server IPs, SSH map, secret locations) — never publish
docs/infrastructure/handoff-access.md

# Decoy page for NL node — must not be publicly linkable to this repo
docs/plug/nl-decoy.html

# Remnawave operations doc (IPs, node details) — local only
docs/infrastructure/remnawave-operations.md
